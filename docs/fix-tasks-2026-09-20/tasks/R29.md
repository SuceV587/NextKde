# R29 · 收尾零散项（P2 打包）

| 字段 | 值 |
|---|---|
| 阶段 | 五 |
| 并发组 | N（子项散文件，内部按文件冲突再细分） |
| 依赖 | 视子项（多数独立） |
| 优先级 | P2 |
| 分支 | `fix/2026-09-20-cleanup` |

## 说明
把各审查报告里小而独立、未单列的项打包。每项独立 commit，逐条验收。可再拆子任务并发。

## 子项清单

### lockscreen / sddm
- ☑ `apps/lockscreen/contents/lockscreen/LockScreen.qml:322-328` — 1.5s 抢焦点 Timer 与 `:428-433` 5s 壁纸树 DFS，屏幕 blanked(`viewVisible=false`) 时仍跑 → 加 `running: viewVisible`。
- ☑ `apps/sddm/theme.conf` `showClock=true` 在 `Main.qml` 从未读 → 实现或删配置项。
- ☑ `apps/lockscreen` README 手动 `kwriteconfig6` 命令与 `kosctl install_lockscreen` 不一致 → 文档指向 kosctl。

### apps/common
- ☑ `ApplicationRunner.cpp:105-110` — 每 app 每 2.5s 轮询 `KWindowEffects::isEffectAvailable` 永不停止 → 连续 N 次相同结果后停，或监听 KWin 信号。
- ☑ `apps/music` `ApplicationActivation.cpp:65` 同步 D-Bus `call`（25s 超时兜底）→ 可接受，记录即可。

### data-service（R3 未覆盖的）
- ☑ `main.go:1048` `bufio.Scanner` 默认 64KB 行上限 → `scanner.Buffer(1MB)` + `scanner.Err()` 回结构化错误。
- ☑ `main.go:1042` `Accept` 出错即 return，listener 永久死亡 → 记录并退出/重 listen。
- ☑ `main.go:1115` `desktopReconcile` 每 5s 全量重扫+重解析 `.desktop` → 按 ModTime 缓存，放宽到 30-60s。
- ☑ `main.go:1122` 无 SIGTERM handler → `signal.NotifyContext` 做最后一次 persist。
- ☑ `main.go:724` `readRaw` `[]byte(string(b))` 双重转换 → `bytes.TrimSpace`。
- ☑ `seedJournalHistory` 启动 ~170 次 `journalctl` fork → `journalctl --list-boots` 单次解析。

### pim-service
- ☑ `main.cpp:33-40` 1Hz 轮询 `bus.isConnected()` → 断开信号或 systemd `Type=dbus`+`BusName`。
- ☑ `PimStore.cpp:600-602` `deliveredReminders` `clear()` 丢当轮判重 → 复核语义。

### KWin / 平台
- ☑ `contextmenuinputeffect.cpp:51` `installPointerSpy` 失败 100ms 无限重试无上限 → 加尝试上限/指数退避；`:124-144` 每次 pointer press 都 `findToplevel`+D-Bus send → 可加"菜单可见"门控。
- ☑ `PlatformServer.cpp` `session.*` `switch-user` 走 `dm-tool`（多发行版不存在）→ 失败映射已可，文档可补。

### tools / 死代码
- ☑ `tools/kosctl:1164` `kos-platform shortcuts` 子命令是死代码（`main.cpp:24-27` 只收 `daemon`），`KOS_SHORTCUTS_FILE` 指向不存在文件 → 实现分支或删除。
- ☑ `IconAppearanceService.qml:82-83` `running=true` 先于 `connect(exited)` → 换顺序（先 connect 再 running）。
- ☑ `settings/main.qml` 4367 行单文件 → 拆 `pages/*.qml`（建议性，工作量大可单列）。

### 一致性
- ☑ `platform.v1.md` 与实现漂移（`network.connect-enterprise`、`session.*` action 范围）→ 同步文档。
- ☑ `ArtworkColorSource.qml:99` `$$.tmp.<pid>` 孤儿文件无 GC → 加清理。

## 验收
- 逐条对照验收；凡改代码的子项均需 `kosctl build`+相关测试+子代理审查。

## 完成定义（强制）

- ☐ 实现 + 验证通过后，**必须**由本代理把 `git diff` 交给只读 `code-reviewer` 子代理审查（fixer 无法派发子代理，留给主代理送审），
  通过才算完成；未通过 → 修复后重新送审。`fixer` 自身验证不算完成。
