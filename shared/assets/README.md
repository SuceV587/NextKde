# Shared assets

Icons, images, and other static assets used by more than one independent
application or by both `shell/` and `apps/`. Empty for now — nothing has
needed a shared asset yet; add files here once one does, instead of
duplicating them per consumer.
