# Shared contracts

Versioned IPC and persisted-data schemas consumed by more than one process
(e.g. the `appearance-settings` IPC contract between `shell/` and
`apps/settings`). Empty for now — existing contracts are still documented
inline next to their implementation; move a contract here once a second
consumer needs the same schema.
